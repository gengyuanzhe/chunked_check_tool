### 背景
我开发了一个S3存储系统，这个存储系统由于无法正确识别 `X-Amz-Content-Sha256` 这个header，导致对于aws-chunked类型的请求，没有解析请求body体里 `length; chunk-signature=xxxx`/ `x-amz-checksum-**` / `x-amz-taiiler-signature`等格式，把整个内容写入到body体里，存储到系统中


### 需求
现在我需要编写一个go语言的工具，能够并发地列举+检查对象，并发结果输出到一个文件里

- 输入: 命令参数 thread bkt prefix nextmarker
- 输出: 只处理普通文件，把损坏的普通文件名写入到一个文件中， 把多段文件名输出到一个单独的列表中

**校验逻辑：**
 
仅对普通对象进行校验，首先通过range读，获取前128字节的内容，并从header中得到etag，根据etag判断是普通对象还是多段，如果是普通对象，则检查这128字节是否以`length;chunk-siganture=xxx\n`这个格式开头


**列举模式**

提供两种列举方式，但都需要保证列举的并发上限
1. 获取当前目录的子目录（使用delimiter），对于每个子目录，循环nextmarker的方式列举
2. 所有列举都是用delimiter，然后把子目录放到队列中，所有并发从队列里取子目录，持续使用delimiter的方法一层层列举

列举出的结果，放到另一个队列里，再用另外一组并发线程来进行校验

配置文件包含如下信息
```
ip:port列表
ak
sk
list_type(nextmarker列举，还是delimiter列举)
列举的并发度
校验的并发度
输出结果目录（默认当前目录） 
is——check(是否仅列举不校验)
is_success_log(正常的对象是否记录到文件中)
```

其他：

1. 程序应当限定各个线程轮流选一个节点发送请求，一旦确定，线程发送请求的节点保持固定，若某个节点故障，则打印报错，并把这个节点先忽略
2. 需要统计list的时延和程序运行的总时延
3. 如果列举/校验失败，需要把结果输入到另一个文件里，支持后续另行处理

**etag区分**

- 普通对象etag: <32 char>

- 多段对象etag，其中2为多段的段数: <32 char>-2